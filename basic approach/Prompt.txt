Analyze this BOM CSV.

Infer any hierarchy or grouping.

Return ONLY valid JSON in this generic graph format:

{
  "nodes": [
    {"id": "string", "label": "string"}
  ],
  "edges": [
    {"from": "string", "to": "string", "type": "contains"}
  ]
}

Rules:
- Every unique item must be a node
- Use "contains" edges for assemblies or groupings
- If no parent is known, connect to a virtual PRODUCT node
- Do NOT invent engineering semantics
- Only represent structure

BOM:
<CSV HERE>
